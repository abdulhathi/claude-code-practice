# Server

Bun is the runtime and package manager for this project.

- Use `bun run index.ts` to start the server
- Use `bun --hot index.ts` for development with hot reload
- Use `bun add <package>` to install dependencies
- Bun automatically loads `.env`, do not use dotenv
- Use `bun test` for tests
