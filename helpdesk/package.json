{
  "name": "helpdesk",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "bun run dev:server & sleep 1 && bun run dev:client",
    "dev:client": "bun run --cwd client dev",
    "dev:server": "bun run --cwd server dev"
  }
}
